# PUBLIC. The site is indexable — reversed at launch, 2026-08-31, per
# fuchsia-studio#1480 and docs/product/ship-sequence.md.
#
# ⚠️ The <meta name="robots" content="noindex,nofollow"> tags came off the same six
# pages in the same change. Both halves moved together, which is what the previous
# version of this comment required. 404.html KEEPS its noindex deliberately: an
# error page should never be indexed, and that is not a leftover.
#
# Tester-phase history: fuchsia-studio#591 (the site described an unreleased build).
User-agent: *
Allow: /
